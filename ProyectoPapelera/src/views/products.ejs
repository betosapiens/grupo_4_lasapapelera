<!DOCTYPE html>
<html lang="en">
    <%- include('./partials/head') %>

<%- include('./partials/header') %>
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">


    <title><h1>Listado de productos</h1></title>
</head>
<body>


<h1>Listado de productos</h1>

<%- include('partials/head', {title: 'Listado de Productos'}) %>

<% for (let i = 0; i < products.length; i++) { %>
    <div>
        <h2><%= products[i].name %></h2>
        <p><%= products[i].description %></p>
        <p><%= products[i].price %></p>
        <img src="images/<%= products[i].image %>" alt="<%= products[i].name %>" width = 400>
        
        <button type:submit class="buttonDetalle" >
        <a href="/products/detail/<%= products[i].id %>">Ver detalle</a>
    </button>
        <button type:submit class="buttonEdit">
        <a href="/products/edit/<%= products[i].id %>">Editar</a>
    </button>
        <button type:submit class="buttonDelete">
        <a href="/products/delete/<%= products[i].id %>">Eliminar</a>
    </button>
    
    </div>
<% } %>
<button type:submit class="buttonCrearProducto">
<a href="/products/create">Crear producto</a>
</button>


<%- include('./partials/footer') %>

</body>
</html>